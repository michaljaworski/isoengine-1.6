#ifndef _MAPLOADER_HPP
#define _MAPLOADER_HPP

#include <SFML/Graphics.hpp>

class MapLoader{
private:


public:
    MapLoader();
    ~MapLoader();

    void mapDraw(sf::RenderWindow* rw);

};

#endif
